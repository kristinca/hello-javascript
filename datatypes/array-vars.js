var change = ['this', 'is', 'an', 'array'];

var arrayconst = new Array('this', 'is', 'an', 'array');

var ch = document.getElementsByClassName('b1');

//ch.textContent = change[0];

ch.innerHTML = arrayconst.item(0);